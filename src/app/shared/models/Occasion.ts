export interface Occasion {
    id: string;
    // next featue
    // artist_image_url?: string,    // muvesz
    user_id?: string,
    description: string;
    date: number;
    image_url?: string;
}